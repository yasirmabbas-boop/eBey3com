import UIKit
import Capacitor

class CustomViewController: CAPBridgeViewController {
    // Intentionally left minimal.
    //
    // Note: Avoid forcing the WebView to fill `view.bounds` or zeroing insets here.
    // That behavior can override Capacitor StatusBar safe-area handling and cause
    // content to render under the notch / Dynamic Island.
}
