node_modules
dist
.DS_Store
server/public
vite.config.ts.*
*.tar.gz
ebey3-67950-firebase-adminsdk-fbsvc-cf76a606aa.json
ebey3_backup.sql
*.sql

# Firebase
.firebase/

# Mobile app (separate project, do not push)
ebey3-mobile

# Android signing credentials (never commit these)
android/key.properties
release-key.jks
*.jks

# Firebase service account (server-side secrets)
server/service-account.json

# Test archives (not for production)
server/_test-archive/
